name: "GitHub Pages - Rust doc"

on:
  push:
    branches:
      - main

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  docs:
    uses: darbiadev/.github/.github/workflows/github-pages-rust-doc.yaml@068870f051676db9e2651013f7c7196ffdaeadaa # v2.0.0
